{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command') && echo \"$cmd\" | grep -qE 'cargo (build|test)' && echo \"$cmd\" | grep -qv 'update-snapshots' && cd /home/user/tractor && RUSTC_WRAPPER=\"\" cargo run --bin update-snapshots --quiet 2>/dev/null || true",
            "timeout": 120,
            "statusMessage": "Updating snapshots..."
          }
        ]
      }
    ]
  }
}
