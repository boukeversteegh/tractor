---
title: YAML Output Reuses JSON Value Tree
type: note
priority: 2
---

YAML output reuses the same JSON value tree produced by the serialization rules,
so all rules apply equally to both `-f json` and `-f yaml`.
