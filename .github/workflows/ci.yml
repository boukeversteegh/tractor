name: CI

on:
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            code:
              - 'tractor/**'
              - 'tractor-core/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'tests/**'
              - 'installer/**'
              - '.github/workflows/ci.yml'

  test:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
      - uses: mozilla-actions/sccache-action@v0.0.7
      - uses: Swatinem/rust-cache@v2

      - name: Run all tests (unit + CLI integration, release mode)
        run: cargo test --workspace --release

      - name: Run snapshot tests
        run: cargo run --release --bin update-snapshots -- --check

  test-windows:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
      - uses: mozilla-actions/sccache-action@v0.0.7
      - uses: Swatinem/rust-cache@v2

      - name: Run unit + library tests
        run: cargo test --workspace --release --lib

      - name: Run tractor-core integration tests
        run: cargo test -p tractor-core --release --test integration_tests

      - name: Run CLI integration tests (skip path-sensitive output tests on Windows)
        run: cargo test -p tractor --test cli --release -- --skip run_cmd --skip formats

  lint:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
      - uses: mozilla-actions/sccache-action@v0.0.7
      - uses: Swatinem/rust-cache@v2

      - name: Build tractor
        run: cargo build --release -p tractor

      - name: Lint
        run: ./target/release/tractor run .tractor.yml

  gate:
    needs: [test, test-windows, lint]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - run: |
          if [[ "${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "One or more jobs failed or were cancelled"
            exit 1
          fi
