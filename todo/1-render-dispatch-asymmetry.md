# Render dispatch asymmetry: query vs check

## RESOLVED

Already fixed. `render_query_report` was moved to `format/mod.rs` alongside
`render_check_report` during the "Clean pipeline separation" refactor. Todo was stale.
