<?xml version="1.0" encoding="UTF-8"?>
<file>
  <comment>// Go distinguishes defined types (`type MyInt int`) from type aliases</comment>
  <comment>// (`type Color = int`). Defined types create a distinct type with its</comment>
  <comment>// own method set; aliases are just a new name for the same type.</comment>
  <comment>//</comment>
  <comment>// Defined type -&gt; &lt;type&gt; (Go's own spec term; inner &lt;type&gt; is the</comment>
  <comment>// underlying type reference).</comment>
  <comment>// Alias        -&gt; &lt;alias&gt; (parallel with Rust / TS / C# / Java).</comment>
  <package>
    package
    <name>main</name>
  </package>
  <type>
    <exported/>
    type
    <name>MyInt</name>
    <type>
      <name>int</name>
    </type>
  </type>
  <alias>
    <exported/>
    type
    <name>Color</name>
    =
    <type>
      <name>int</name>
    </type>
  </alias>
</file>
