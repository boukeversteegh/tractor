|
  class[public] (2)/ = "public class"
    ├─ body (2)/ = {…}
    │   └─ method[public] (5)/ = "public", "(", ",", ")"
    │       ├─ body (5)/
    │       │   └─ block (5)/ = {…}
    │       │       └─ … (12 children)
    │       ├─ name (5) = "Add", "Subtract", "Multiply", "Hello", "Goodbye"
    │       ├─ parameter (8)/
    │       │   ├─ name (8) = "a", "b", "name"
    │       │   └─ type (8)/
    │       │       └─ … (1 children)
    │       └─ returns (5)/
    │           └─ type (5)/
    │               └─ … (1 children)
    └─ name (2) = "Calculator", "Greeter"

  (use -d to increase depth, or -x to query specific elements)
